<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.SuperMi" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/accent</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorPrimaryContainer">@color/accent_soft</item>
        <item name="android:windowBackground">@color/page_bg</item>
    </style>

    <style name="Theme.SuperMi" parent="Base.Theme.SuperMi" />
    <style name="Theme.SuperMi.Transparent" parent="Theme.SuperMi">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <!-- 截图查看器：透明窗口从气泡位置展开，背景/状态栏颜色由页面自己淡入 -->
    <style name="Theme.SuperMi.Viewer" parent="Theme.SuperMi.Transparent">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>
</resources>
